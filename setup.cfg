[metadata]
name = alteria-cli
version = 0.1.0

[options]
packages = alteria_cli
install_requires =
    click>=8.0
    prompt_toolkit>=3.0

[options.entry_points]
console_scripts =
    alteria = alteria_cli.main:cli
